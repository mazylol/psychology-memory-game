import type { Actions } from './$types';

// idea to handle form action:
// have the form be submitted at every number submit.
// If the round is 2 and the answer was wrong, then add the result to the database.
